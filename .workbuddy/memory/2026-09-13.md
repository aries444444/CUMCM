# 2026-09-13

## 重装 scientific-illustration-guide 并重做架构图

- 用户要求重新拉取仓库最新版 skill（不用缓存）。经 codeload 重新下载 wentorai/research-plugins，diff 确认上游 SKILL.md 与已装版本逐字节一致（内容无变化），仍按用户要求删除旧副本并重装。
- 注意：`ProblemFolder/figures_pub/` 在此期间被清空（昨天的 fig01-12 与脚本均已不在；ProblemFolder 下出现用户自己生成的 fig1_overview_solution.png 等）。已重建 `make_architecture.py`。
- 按 skill 的色盲无障碍规范修正配色：模型检验层由红色改为中性石板灰（#ECEFF1/#455A64），避免红+绿配对区分不同类别；耦合箭头保留红蓝（同一机制内部编码，非类别区分）。
- 重新生成 `fig13_模型总体架构图.png/.pdf`（300dpi + 矢量），五层结构不变：输入 → 热-质耦合 PDE（含动边界）→ 数值求解 → 问题1-4 输出 → 模型检验反馈闭环。
- 环境备忘：MPLCONFIGDIR 需指向 Temp\mplcfg（沙箱不允许写 C:\Users\Administrator\.matplotlib）；GitHub 大文件用 codeload zip 下载（git clone 受 schannel 吊销检查失败影响）。
