# 2026-09-12

## 科研可视化 skill 安装与 A 题配套静态图

- 安装科研可视化 skill：`alterlab-scientific-viz`（用户级 `~/.workbuddy/skills/alterlab-scientific-viz`，来源 skills.sh / GitHub alterlab-ieu）。安全审计通过（P2，纯 matplotlib 工具，无网络/凭据行为）。
- SkillHub（lightmake.site）语义搜索对本需求基本无效（返回低相关热门 skill）；Vercel `npx skills find` 有效。
- 坑1：`npx skills add` git clone 失败（schannel CRYPT_E_NO_REVOCATION_CHECK），改用 `curl -L codeload.github.com/<owner>/<repo>/zip/refs/heads/main` 下载 zip 手动安装。
- 坑2：沙箱禁止写 `C:\Users\Administrator\.matplotlib`，运行绘图脚本须设 `MPLCONFIGDIR=C:\Users\Administrator\AppData\Local\Temp\mplcfg`。
- 绘图环境：`~/.workbuddy/binaries/python/envs/default`（venv）已装 pandas/matplotlib/seaborn/openpyxl；中文字体用 Microsoft YaHei。
- 产出：`ProblemFolder/figures_pub/` 12 张出版级图（300dpi PNG + 矢量 PDF）+ 可复现脚本 `make_figures.py`。数据基准：result1-4.xlsx 与附件1/2.xlsx；关键数值已核对（Q1 表面 30 min = 36.79°C、Q3 t*=57.09 h、Q4 t*=50.77 h、表7 灵敏度数值）。
- 技巧：含水率时空热图动态范围大，用 `PowerNorm(gamma=0.45)` 提升低浓度区对比度；Q4 热图 NaN 掩膜自然呈现收缩动边界。
- 论文 `A题论文.tex` 目前只有表 1-7、无插图环境；如要插图可引用 figures_pub 下 PDF。
